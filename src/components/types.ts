
// This is a placeholder file to maintain correct import paths
// We'll add proper types here as needed in the future

export interface ReportSection {
  title: string;
  content: React.ReactNode;
}

